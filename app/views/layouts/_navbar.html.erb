<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="/">ELearning</a>

  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item <%= current_page?(root_path) ? 'active' : '' %>">
        <%= link_to "Home", root_path, class: "nav-link" %>
      </li>
      <% if user_signed_in? %>
        <li class="nav-item <%= current_page?(current_batches_path) ? 'active' : '' %>">
          <%= link_to "My Batches", current_batches_path, class: "nav-link" %>
        </li>
        <% if current_user.admin? || current_user.schooladmin? %>
          <li class="nav-item <%= current_page?(admin_enrolment_requests_path) ? 'active' : '' %>">
            <%= link_to "Admin", admin_enrolment_requests_path, class: "nav-link" %>
          </li>
        <% end %>
        <li class="nav-item">
          <span class="navbar-text">Signed in as <%= current_user.email %></span>
        </li>
        <li class="nav-item">
          <%= link_to "Sign out", destroy_user_session_path, method: :delete, class: "nav-link" %>
        </li>
      <% else %>
        <li class="nav-item">
          <%= link_to "Sign in", new_user_session_path, class: "nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to "Sign up", new_user_registration_path, class: "nav-link" %>
        </li>
      <% end %>
    </ul>
  </div>
</nav>
